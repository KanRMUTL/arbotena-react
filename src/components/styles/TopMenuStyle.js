import styled from 'styled-components'

export const Wrapper = styled.div`
    width: 100%;
    margin: auto;
    padding-top: 30px;
    padding-left: 3.5vw;
    display: flex;
    justify-content: space-between;
    z-index: 10;
    position: absolute;
    `;